<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <h3 class="text-3xl mb-1">Hello, <%= user.name %> 👋🏻</h3>
        <h5 class="mb-5">you can create new a post.</h5>
        <form action="/post" method="post">
            <textarea placeholder="what's in your mind?" name="content" id="content" class="w-1/3 p-5 resize-none bg-transparent border-2 border-zinc-800 rounded-md block"></textarea>
            <input class="w-40 px-3 py-2 bg-blue-500 rounded-md mt-5" value="Create Your Post" type="submit">
            <a class="bg-red-500 rounded-md px3 py-2 p-3" href="/logout">Logout</a>
        </form>
        <div class="post mt-20">
            <h3 class="text-zinc-400">YOUT POSTS</h3>
        </div>
        <div class="postcontainer mt-5">
            <% user.post.reverse().forEach(function (post) { %>
                <div class="post w-1/3 p-4 mb-2 border-2 rounded-md border-zinc-800 bg-zinc-800">
                    <h3 class="text-xl text-blue-500 mb-2">@<%= user.username %></h3>
                    <p class="text-sm traking-tight"><%= post.content %></p>
                    <small class="mt-2"><%= post.likes.length %> likes</small>
                    <div class="btns flex mt-1 gap-3 inline-block">
                        <a href="/like/<%= post._id %>" class="text-blue-500">
                            <%= post.likes.indexOf(user._id) === -1 ? "like" : "unlike" %>
                        </a>
                        <a href="/edit/<%= post._id %>" class="text-zinc-500">edit</a>
                    </div>
                </div>
                <%  }) %>
        </div>
    </div>
</body>
</html>