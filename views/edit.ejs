<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <h5 class="mb-5">Edit your post</h5>
        <form action="/update/<%= post._id %>" method="post">
            <textarea placeholder="what's in your mind?" name="content" id="content" class="w-1/3 p-5 resize-none bg-transparent border-2 border-zinc-800 rounded-md block"> <%= post.content %> </textarea>
            <input class="w-40 px-3 py-2 bg-yellow-500 text-black rounded-md mt-5" value="Update Your Post" type="submit">
        </form>
    </div>
</body>
</html>